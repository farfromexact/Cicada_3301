# Liber Primus 未解页自动循环审查：R009 起始登记

日期：2026-09-13（Asia/Shanghai）  
前轮：R008/H016-v2 已完成；v1验收无效的原始运行保留，v2只有有限页兼容性且无候选。  
本轮预算：最多3个有界方法实验；每个实验120秒单独截止；完成一轮后再按结果决定是否进入下一轮。  
输入边界：先查 `STATE.md`、`hypotheses/`、`runs/`、`reviews/`、`research/knowledge.json`、`research/METHOD.md` 和 `feeds/`；不联网、不启用后台任务、不执行未审查上游脚本。

## R009 三方向与信息增益排序

1. **H017 一阶密文反馈（选中）**：固定每页虚拟前项 `C[-1]=0`，即 `P[0]=C[0]`、`P[i]=C[i]-C[i-1] mod29`；逐页重置，非符文不参与，F按普通符文处理，不叠加H016的F例外。用已有H009校准过的训练-only add-one unigram分数，仅对 `P[1:]` 评分；覆盖全部55个含符文页，999个整页密文置换做maxT。它不引入钥匙、偏移或可调跳位，能够直接证伪“反馈本身产生可检出的文本结构”。
2. **H018 四阶乘数子群投影（未选）**：固定 `V(x)=x^4 mod29` 与四阶单位根集合 `{1,12,17,28}`，检验零掩码和八类投影（零加七个非零类）顺序关系。它与H011的平方剩余投影相关但不相同，仍需独立功效门控，不能直接继承H011阴性。
3. **H019 几何边界自生钥匙（未选）**：只考虑由明确外环版面预测内环的单一因果规则；当前外环16格、内环8格尚不能唯一确定起点、方向、表示和长度处理，若先跑会把版面自由度变成后验调参，因此暂缓。

Astra（只读）给出的排序是 A ＞ C ＞ B：H017与既有外生钥匙、F状态和H008相邻相等扫描不重复，且最容易用独立正向累加/反差分验证；H018需防止把相关投影重复计证；H019尚未通过取钥唯一性门控。该排序是实验设计判断，不是密码学证据。

## H017 冻结要求

规范见 `hypotheses/H017-ciphertext-feedback-v1.json`。worker只接收公开密文页，私有合成正例只在验证端使用。独立端重新实现邻差，不调用worker的变换函数；验证四个条件：全部55页顺序、两个正例精确回差分、内部单点扰动的 `+delta/-delta` 局部性、以及控制行的固定宽度/顺序。

统计输出把 `lead` 与 `not_detected` 分开；没有lead才可对这个固定反馈模型和置换零假设写 `negative`。任何lead只进入候选线索，不是解密；需要另行冻结独立明文或结构预测。999行控制逐行写入partial JSONL并写原子检查点，超时保持`timeout`，不产生阴性。

## H017 实际结果

正式运行见 `runs/auto-cycle-r009-H017-v1/record.json`、`statistics.json`、`independent-verification.json`、`control-checkpoint.json` 和复现包，状态为 `negative`：55个含符文LP2页、12956符文、55个评分单元和999/999页内完整密文置换对照全部完成；两个合成正例精确回差分，单点扰动局部性和worker读隔离均通过。

没有 `lead` 或 `unsolved_page_candidates`。最强项为 LP2/5，反馈输出的训练-only unigram 分数为 -3.9393，maxT校正 `p=0.016`，接近但仍高于预注册的0.01门槛。该阴性只约束固定 `C[-1]=0`、逐页重置、F普通处理、首位不计分的差分变换、训练-only scorer 和全页置换零假设；不排除其它反馈阶数、初值、边界或复合机制。H018/H019仍未执行。
