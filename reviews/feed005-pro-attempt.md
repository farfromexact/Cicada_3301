# FEED-005：外部 Pro 尝试 EXT-PRO-001

这是一份可追溯的外部尝试报告，当前状态 inconclusive。它值得保留，尤其是明确拒绝把高分乱码当作部分破解。
但尚无可重跑的原始运行材料，因此不能作为仓库已复验的阴性实验，也不增加 knowledge.json 的已验证主张或实验数量。

通过应用 read_thread 读取同名对话“解读Liber Primus”，得到一轮用户请求和最终回答；完整返回 JSON 已保存。
用户提供的浏览器 WEB URL 与应用 conversation ID 不同；浏览器连接两次失败，未独立确认二者映射。
同名标题、时间与“LP2 第一页”的请求内容支持这次匹配，仍明确保留该来源限制。
includeOutputs=true 未返回程序、工具执行日志或附件。回答中的引用 ID 和 content-reference 不是已取得的证据文件。

报告自述：目标 LP2 原始编号 0.jpg，共 262 符文；1,263,576 组参数，包括等价/重复变换。
所列家族包括可逆仿射、长度 1—3 循环密钥、二次多项式流、若干整数序列及 37 个关键词。
精确方向、序列起点、变体列表、重置和 F 规则、评分器、接受门槛及原始输出均未提供。
不能用参数总数推断独立覆盖，更不能推出长密钥、换位、所有 F 子集或任何整个密码族已排除。

该回答自述已通过 Atbash 和 56 页控制，与本仓库既有回归目标重合，但不是对它那份代码的独立验证。
“Pro”来自用户陈述，工具无模型版本元数据；投入多少推理不等于覆盖证明。
本地只复跑 AGENTS 要求的既有回归；这次接入没有新增未解页候选。另一个任务“记录破解实验尝试1”拥有 FEED-004/H006，勿合并计数。

后续有价值的补证是原始脚本、密文文件、枚举规则、评分验收和运行日志；拿到后先固定哈希，审查代码，再选择有鉴别价值的最小复验。
若取不到，就保留“报告尝试过、覆盖未验证”，未来查询命中时提示可能重复，而不是自动跳过这些假设。

接入回归命令：`python -X utf8 scripts/run_round.py`，进程退出码 0，记录 runs/20260913T100656.661332Z/record.json。
tests、reproduce、research、clues、math、synthetic 均完成且退出 0。运行 ZIP 固定了当时包括并行任务文件的工作区快照。
这些通过项验证仓库完整性与既有协议，不验证外部报告的 1,263,576 次执行。
