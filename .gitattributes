# Preserve hashed artifacts byte-for-byte, including Windows line endings.
* -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
# Source snapshots and raw execution evidence are immutable, not style targets.
sources/** -whitespace
runs/** -whitespace
