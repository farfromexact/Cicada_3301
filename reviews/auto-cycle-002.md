# Liber Primus 未解页自动循环审查：R005 起始登记

日期：2026-09-13（Asia/Shanghai）  
前一轮：R001–R004 已达到4轮上限；H009–H012及其配置不得重复。  
本续轮预算：最多3轮、每轮120秒、总实验预算360秒；发现LP2候选时停止并转入独立复现。  
输入边界：只读仓库内 `STATE.md`、`hypotheses/`、`runs/`、`reviews/`、`research/knowledge.json` 和 `feeds/`；不联网、不启用后台任务、不执行未审查上游脚本。

## R005 三方向与选择

1. **H013 固定仿射泛化（选中）**：把已经在LP1/06来源回归中固定的 `(a,b)=(28,2)` 应用于LP2全部55个含符文页；以H009训练-only unigram均值和256个随机可逆仿射对照做预注册maxT诊断。可证伪预测是固定映射在至少一页达到校正后 `p<=0.01`；无独立明文时只算候选线索，不算解密。选择理由是它不增加参数搜索，却能直接检验H012向未解页迁移的核心预测。
2. **CIRCUMFERENCE 连续 Vigenère 回归（未选）**：对LP1/14–15的来源转写、key `FIRFUMFERENFE` 和skip 65/76做已知页逐符文复现，再考虑是否有依据迁移；可证伪但需要重新冻结连续边界、歧义和独立来源。当前先不做。
3. **几何/数阵密钥流检出能力（未选）**：在有固定布局的LP1/05或LP1/16上预注册顺序敏感读序与已知DIVINITY/F机制正负控制；若正例不能被结构统计检出，则不把H008阴性用于未知页解释。当前结构定义和图像级独立审计不足，先不做。

选择前已查明：H012运行的coverage明确为`known_page=LP1/06`且`unsolved_page_candidates=[]`，因此H013是新范围，不是重复运行。H013冻结规范见 `hypotheses/H013-affine-generalization-v1.json`。

## R005 实验状态

R005 已完成：H013 在55个LP2适用页、12956个符文上测试固定 `(a,b)=(28,2)`，256个随机可逆仿射对照和独立回算均完成；结果 `negative`，最强 LP2/5 校正后 `p=0.599`，`unsolved_page_candidates=[]`。阴性仅限固定 reverse+3 迁移预测、训练-only unigram 与仿射控制零假设。

## R006 三方向与选择

R005 无候选，因此进入新的方法族决策：

1. **H014 CIRCUMFERENCE 固定钥匙流迁移（选中）**：先用来源 LP1/14–15 的 `FIRFUMFERENFE`、连续块和工具输入一基字符坐标65/76（推导为零基符文ordinal49/58）做226符文精确门控；门控通过后，测试LP2四个固定的连续/逐页重置 × F消费/不消费分支。可证伪预测是至少一个分支在999组页内置换maxT后 `p<=0.01`。这是新钥匙族，不重复 H001 的 DIVINITY 或 H013 的仿射映射。
2. **FEED-009 P2 离散对数/模4/7投影（未选）**：需要先补出固定乘数/指数的来源和复用单位；直接换坐标会与H010/H011相关，暂不重复统计。
3. **几何数阵密钥流能力校准（未选）**：用LP1/05、LP1/16固定数阵和已知机制正负控制检验顺序敏感路径是否可检出；图像级独立审计和密钥推导尚未冻结，信息增益低于来源钥匙门控。

H014 冻结规范见 `hypotheses/H014-circumference-transfer-v1.json`；执行后继续追加实际覆盖、状态分类、随机对照、独立复算和阴性范围。未执行方向不写成negative。

## R006 实际结果

H014 首次运行在独立回加密实现处报 `error`，`runs/auto-cycle-r006-H014-v1/` 保留；定位为 F-free 逆向时把“输入密文 F”错误实现成“明文 F”。修正后在新路径 `runs/auto-cycle-r006-H014-v1-retry/` 完成：来源门控226/226精确，LP2四分支220单元、999组页内置换对照、51824个符文独立校验全部完成；结果 `negative`，最强 LP2/40 `page_reset_f_consume` 校正后 `p=0.715`，`unsolved_page_candidates=[]`。阴性仅限H014的固定钥匙、四分支、评分器和置换零假设。

## R007 三方向与选择

R006 无候选，最后一轮在新的有限域映射族中登记：

1. **H015 固定 Möbius 反演（选中）**：唯一测试 `f(0)=0, f(x)=x^-1 mod29`，覆盖全部55个LP2适用页，以256个随机零保持排列做maxT对照。可证伪预测是固定反演在任一页达到校正后 `p<=0.01`；不把0的有限域补丁或得分线索当作解密。
2. **其它 PGL/Möbius 分式线性变换（未选）**：需要极点编码或第30个符号，当前没有来源约束；不展开参数枚举。
3. **FEED-010 几何/数阵自生钥匙（未选）**：需要先完成图像级独立校对和外环→内环的唯一对应规则；当前不具备冻结条件。

H015 冻结规范见 `hypotheses/H015-mobius-inversion-v1.json`。本续轮达到3轮上限后停止；未执行方向不写成negative。

## R007 实际结果

H015 已完成：55个LP2适用页、12956个符文，固定 `f(0)=0, f(x)=x^-1 mod29`，另有256个随机零保持排列对照；固定反演和独立逐符文回算均完成。结果为 `negative`，最强 LP2/48 的校正后 `p=0.544747`，`unsolved_page_candidates=[]`。该阴性只覆盖这一固定反演、unigram评分器和零保持排列零假设；0→0是预先冻结的补丁，不是密码学证据。

## 自动循环 R005–R007 收束

三轮续轮均无LP2候选：H013固定 `reverse+3` 仿射迁移为 `negative`（LP2/5，校正后 `p=0.599`）；H014固定 `FIRFUMFERENFE` 四分支迁移为 `negative`（来源门控226/226精确，LP2/40，校正后 `p=0.715`）；H015固定有限域反演为 `negative`（LP2/48，校正后 `p=0.544747`）。H014首次独立验证错误运行已保留，修正后的retry完成全部预注册覆盖。三批均有随机对照、独立验证和明确的实际覆盖记录，未产生可独立复现的候选解。

按预注册规则在3轮/360秒续轮上限处停止；停止不是“所有高优先级方向均已充分测试”。P2投影、其它PGL分式变换、几何/数阵自生钥匙仍保持未执行，不能写成negative。所有实验、错误日志、统计、验证和复现包均保留；本轮没有把语言模型补写或可读片段计入解密输出。
