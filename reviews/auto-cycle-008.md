# Liber Primus 未解页自动循环：R013 行角色结构诊断

日期：2026-09-13（Asia/Shanghai）  
前轮：R012/H022 的蛇形方向功效门失败并被记为 `inconclusive`；LP2没有科学解释。  
方法边界：继续在几何/结构族内测试一个不同的位置掩码；严禁重复H021同序号相等、H022蛇形方向或H008线性lag。LP2/50仍不转码、不补方阵。

## 三个可证伪方向与选择

1. **H023 行边缘/内部角色差异（选中）**：固定每条长度至少3且未跨硬字面段的原始行，首尾两个符文合为边缘，其余为内部；每页建立29×2列联表并计算固定G统计量。左右边缘合并，不拆第二层、不加GP映射。每行独立循环移位作为零假设，破坏边缘身份而保持行内容和循环邻接。
2. **H022-v2 方向随机化显著性（未选）**：保留固定蛇形规则，但改用全新版本、解析功效校准和真正两阶段dispatch，以随机行方向的经验显著性代替H022-v1的绝对delta门槛。它是H022新协议，不是对v1阈值补考，暂缓以免在刚失败的有向bigram族停留。
3. **H024 行内中心镜像相等（未选）**：固定在每条完整长度至少4的行内比较`j`和`n-1-j`，中心不参与；不补格、不移动中心、不搜索偏移。它没有未解页中心对称的直接来源依据，且与H008部分距离配对相关，后置。

Astra 的只读排序为 H022 ＞ H023 ＞ H024，但H022-v1测量门已失败后，下一步选H023作为结构族内的不同位置角色问题：规则短、统计可独立复算、与H021/H022的配对对象不同。该排序只是信息增益判断，不是密码学证据。

## H023 冻结摘要

输入严格沿用H021的slash行和硬段定义：`/`是原图行界，CR/LF不造行；当前bbox均为`unlocated`，不声称物理像素列。含空行、硬段或网格字面屏障的行不跨越屏障；LP2/49、51只保留未被硬字面截断的符文行，LP2/50无注册符文而不适用。只使用原始0..28序号。

每页对所有合法行累积边缘/内部29×2计数，固定计算`G=2*sum(O*log(O/E))`，零观察项贡献0；不使用语言模型。功效门采用固定48行左右的不等长合成布局：20个正例让边缘以固定概率0.8来自预先固定的7符文集合、内部均匀；99个负例分为均匀、行内频率异质但位置独立、以及另一个位置独立分布类。固定G门槛和0/99误收要求写入H023规范，困难负例分列报告。

正式批次必须两阶段执行：第一阶段worker只收到gate公开原始行；只有gate通过后，第二阶段worker才收到55页LP2公开原始行。999组独立逐行循环移位控制保持行长、内容和循环邻接，maxT校正`p<=0.01`才是结构lead。门失败/超时/覆盖不足均为`inconclusive`，不写negative。

## H023-v1 实际运行与结论

正式运行：[record](../runs/auto-cycle-r013-H023-v1/record.json)、[statistics](../runs/auto-cycle-r013-H023-v1/statistics.json)。

- 状态：`negative`（限定）；功效门`passed`，20/20正例精确通过、99/99负例无误收，三类负例分别为`negative_uniform`、`negative_row_heterogeneous`、`negative_fixed_seven`，各0误收。
- 执行顺序：gate worker只收到119个合成公开job；gate独立验证通过后才创建并dispatch LP2公开job。LP2 worker独立验证通过，未把gate失败路径或同一stage混算进本轮。
- LP2解析覆盖：55/55含符文页、12956符文、55个有效评分单元；实际进入G统计的是578条合法行/12621符文，其中边缘1156、内部11465，335个符文因硬段规则排除。999/999逐行独立循环移位controls，checkpoint标记`complete`且partial JSONL为999行；gate和LP2独立验证分别检查294168和12956个符文。独立重放审计见 [H023 audit](../runs/auto-cycle-r013-H023-v1-audit.json)。
- 统计：无lead、无`unsolved_page_candidates`。最强LP2/26的`G=47.2608283411`、`z=2.6622603273`、maxT校正`p=0.380`；LP2/7为`p=0.617`，LP2/35为`p=0.967`。
- 结论范围：只约束本规范的首尾合并边缘/内部掩码、slash/硬段行定义、pooled 29×2 G统计和逐行循环移位零假设；不涉及物理像素列、其它对齐、中心镜像、蛇形或自生钥匙。LP2/50无注册符文，仍为不适用而非negative。
- 复现：`reproduction-bundle.zip`已保存，运行代码、输入/依据哈希、worker stdout/stderr、独立验证、controls、partial JSONL和checkpoint均在运行目录内。

R011 H021、R012 H022门失败、R013 H023已连续三轮无可独立复现候选；根据实验纪律，下一轮强制离开几何/结构族。H024仅作为历史未执行方向，不在下一轮补考；需重新登记三个非几何候选并选定一个后再跑。
