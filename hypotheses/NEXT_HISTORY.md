# 下一轮方向与已完成机制实验

## 2026-09-19 当前切换点

H030已因全部208合格视图不满足<=16阶必要条件停止补控；保留575/999断点但不再优先恢复。H031-v4共同变换快速筛选没有候选（55页，正式随机控0）；H027共享流恢复门0/20、H026逐词掩码碰撞门8/20均失败而及时停止，没有向LP2下发这两种扫描。

见 `../reviews/20260919-fast-triage.md`、STATE顶部、E029–E033和FEED-012/M009。后续先寻找能独立约束明文/密钥/读序的新证据，不在这些版本上扩阶数、换offset或降阈值补考。来源驱动的新机制先小门控，有可预测苗头才投入全库确认；无法检测合成正例不是LP2密码学阴性。

以下旧暂停点/下一步为历史，不覆盖本节。


## 当前暂停点：H030-v3（2026-09-14）

H030-v3 已在 `next_replicate=575` 的完整 checkpoint 安全暂停：旧 200 条重放通过，gate 20/20、0/99，正式 observed 独立验证通过；总对照只完成575/999，尚无统计结论。恢复前先校验 [record](../runs/auto-cycle-r015-H030-v3/record.json)、[partial/checkpoint](../runs/auto-cycle-r015-H030-v3/control-checkpoint.json) 与 [resume-source](../runs/auto-cycle-r015-H030-v3/resume-source.json)，从575继续，不重发0..574。

H027 相邻页零偏移复用加法流与 H031-v2 全PGL仍未运行；二者都不能写成 negative。H030 完成后再按新审阅决定先后，保持四视图、F29、999对照、holdout与独立验证契约不变。

## 自动循环 R008 H016-v3 验证修订（2026-09-14）

H016-v3 在不改变 H016-v2 科学模型和 maxT 判定的前提下，补齐了真正两页的四个私有正例、8个分页 endpoint 验收、正向未截断短路径 oracle（13个起始相位、328380个长度0–3输入）和统计写盘前后 deadline guard。正式 [运行](../runs/auto-cycle-r008-H016-v3/record.json) `inconclusive`：55页/12956符文、两分支、999/999对照、独立验证通过；LP2/2、15、49、53仍逐页重置兼容，连续分支在LP2/0位置5死亡；无lead或候选，最强 LP2/53 的 maxT `p=0.059`。

H016-v3 只是验证强化和 v2 结果复现，不把四页兼容当作明文或全库兼容。固定 FIRFUMFERENFE、初相位、F策略和两种连续性不得后验调参。下一方向按 Astra 审阅排序为：先完成已有 checkpoint 的 H030-v2 BM 对照（200/999已落盘），再为尚未冻结的 H027 相邻页流消去做合成 gate，最后才考虑 H031-v2 全PGL；三者均须新批次、holdout、随机对照和独立验收。

FEED-011提案接入：在R013后的非几何方向选择中，可优先考虑BM原始F29序列的合成检出能力/前缀预测后缀校准，其后是有类型DSL的状态契约和PGL的分隔符契约。均未执行、未定搜索预算，详见reviews/feed011-composite-mechanisms.md。密文复杂度不等于密钥生成器复杂度；固定∞的PGL仅仿射；深度≤3不能代替参数范围。不得直接执行附件给出的三项批次prompt，或据既有有限阴性声称单层密码已充分排除。

## 自动循环 R013 H023 结果（2026-09-13）

R013 已完成 [H023行边缘/内部G统计](../hypotheses/H023-row-edge-interior-g-v1.json)：20/20正例、99/99负例功效门通过；gate只dispatch合成job，随后才单独dispatch LP2。独立验证通过，LP2覆盖55个含符文页/12956符文/55个评分单元，999/999逐行循环移位控制完成。正式状态为限定 `negative`，无lead或候选；最强LP2/26的maxT校正p=0.380，LP2/7为0.617，LP2/35为0.967。详见 [运行记录](../runs/auto-cycle-r013-H023-v1/record.json)、[统计](../runs/auto-cycle-r013-H023-v1/statistics.json) 和 [R013审查](../reviews/auto-cycle-008.md)。

阴性仅限固定首尾合并边缘/内部掩码、slash/硬段行定义、pooled 29×2 G统计和逐行循环移位零假设；不能推出物理几何或其它版面关系不存在，LP2/50仍为不适用网格。R011–R013已连续三轮无可复现候选，下一轮强制切换出几何/结构族；H024不补考。需先提出三个非几何、非已测冻结参数的方向并预注册，不能直接扩张搜索。

## 自动循环 R012 H022 门控结果（2026-09-13）

H022 的固定交替蛇形有向二符文诊断见 [规范](H022-snake-row-bigram-v1.json)。正式门控为 `inconclusive`：20/20正例达`delta>=0.05`，但随机方向负例2/33误收，违反0/99验收；未进入LP2科学统计，无候选。因一次性dispatch实现曾让worker处理LP2的55页，不能写成物理“未执行”；更正见 [审计记录](../runs/auto-cycle-r012-H022-v1-correction.json)。不调阈值补考。

下一步按 Astra 排序选 H023：固定行首尾边缘/内部的29×2 G统计，采用真正两阶段gate dispatch、独立正负功效校准和999组逐行循环移位对照。H024行内中心镜像相等为备选；H021/H022已经测试或门失败的规则不重复，未执行方向不得写成negative。

## 自动循环 R011 H021 结果（2026-09-13）

R011 在三轮无候选后切换到几何/网格结构族；Astra 排序后选中 [H021](H021-row-alignment-geometry-v1.json)，固定原始`/`行界、左起共同序号和硬/空行屏障，不猜物理像素列。20/20正例、99/99负例（含33个困难负例）功效门通过；LP2 55页/12956符文/55个评分单元、999组逐行循环移位对照、独立验证和归档均完成，结果为限定 `negative`，最强LP2/33的maxT校正p=0.666，无候选。详见 [R011审查](../reviews/auto-cycle-006.md) 和 [运行统计](../runs/auto-cycle-r011-H021-v1/statistics.json)。

阴性只限上述行规则和循环移位零假设；bbox未定位，因此不排除物理列、右对齐、平移、蛇形或非相邻几何。下一候选可执行 H022（固定交替行反向的有向二符文一致性）或 H023（固定行边缘/内部G统计），须保留各自正负功效门和独立验证；不得修改H021后回看结果调参，也不得把LP2/50字母数字网格转换成符文。

## 自动循环 R010 H018-v1/v2 结果（2026-09-13）

H018-v1 的原始 `negative` 因合成功效门零掩码、门失败硬停、截止检查和依据归档问题撤回，见 [修正记录](../runs/auto-cycle-r010-H018-v1-correction.json)。修正后的 [H018-v2](H018-fourth-power-projection-v2.json) 先通过20/20正例、0/20负误收功效门，再完成55页/12956符文、999组零保持置换、独立验证与完整检查点；正式状态为限定 `negative`，无候选，最强LP2/55的maxT校正p=0.958。该结果只约束固定 `x^4` 投影及相邻类相等零假设。

R008/H016-v2、R009/H017、R010/H018-v2 已连续三轮没有可复现候选，下一轮必须换方法族。下一批应优先登记一个具有唯一版面规则、先经正负合成校准的几何/网格顺序诊断；H019 的外环到内环自生钥匙在起点、方向、表示和长度处理未唯一化前不得运行。H018 的指数、阈值、投影类或控制规则不得继续后验调参；所有未执行方向仍不得写成negative。

## 自动循环 R008 H016 v2 结果（2026-09-13）

H016-v1 的原始 `negative` 已由 [修正记录](../runs/auto-cycle-r008-H016-v1-correction.json) 撤回；原因是四个正例检查为空、连续跨页路径重数重置和状态分类混淆。修正后的 [H016-v2](H016-periodic-plaintext-F-state-v2.json) 在55个含符文LP2页、12956符文、两分支和999组固定F位置置换对照下完成，独立验证和四个正例均通过。

v2 结果为 `inconclusive` 而非negative：逐页重置兼容 LP2/2、15、49、53（LP2/49为单终态，其余存在路径歧义），其余51个 page-reset 单元在合法前缀死亡；连续分支在LP2/0零基位置5死亡，后续页为 `not_reached`；最强 LP2/53 的maxT校正 p=0.121，无统计 `lead`、无候选明文。详见 [R008审查](../reviews/auto-cycle-003.md)、[统计](../runs/auto-cycle-r008-H016-v2/statistics.json)。

H016 的固定钥匙和F双路径模型不再重复调参。下一轮须切换方法族并新建冻结规范；候选为有明确形式定义的一阶密文反馈、几何/数阵自生钥匙，或先做独立功效门控的投影。所有未执行方向仍不得写成negative。

## 自动循环 R009 H017 结果（2026-09-13）

在查明 H016-v2 的局部兼容但无候选后，Astra 将三方向排序为：一阶密文反馈 ＞ 四阶乘数子群投影 ＞ 几何边界自生钥匙。已先完成 [H017](H017-ciphertext-feedback-v1.json)：固定 `C[-1]=0` 的页内一阶差分、首位不计分、训练-only unigram、999组整页密文置换，55页/12956符文及两个合成正例全部通过独立验收；最强 LP2/5 的maxT校正 p=0.016，无 `lead`、无候选，故为该精确模型的限定 `negative`。详情见 [R009审查](../reviews/auto-cycle-004.md) 和 [运行统计](../runs/auto-cycle-r009-H017-v1/statistics.json)。

H017不排除其它反馈阶数、初值、边界或复合层。下一步若继续，须先为 H018 的 `x^4 mod29` 四阶子群投影做独立功效门控，再决定是否覆盖LP2；H019仍缺少唯一的外环→内环取钥规则。未执行方向不能写成negative。

## 自动循环 R005–R007 收束（2026-09-13）

续轮先查历史，未重复H009–H012；连续完成三轮新冻结实验：H013将LP1/06已知页的固定reverse+3迁移到LP2全部55页，negative；H014在CIRCUMFERENCE来源门控226/226后测试固定钥匙流四分支，修正独立F-free逆向错误后negative；H015测试唯一冻结的 `f(0)=0, f(x)=x^-1 mod29`，negative。三批均完成预注册随机对照、独立验证和实际覆盖，均无LP2候选。详见 reviews/auto-cycle-002.md、STATE.md、research/knowledge.json 的E014–E016。

本续轮在3轮/360秒上限停止，不把未执行方向写成negative，也不声称高优先级方向已穷尽。下一次若继续必须新建实验版本；优先保留P2离散投影、其它PGL分式变换、以及先做图像级审计和正负控制的几何/数阵自生钥匙路线。H013–H015的冻结配置不得重复。

## 自动循环 R001–R004 收束（2026-09-13）

自动循环已完成4轮，达到预设4轮/480秒上限，当前无LP2未解页候选。H009已完成H003所需的评分器校准（20/20真恢复、0/20随机误收）；H010乘法素数时钟在55页、220单元、999对照下为scoped negative；H011 Legendre投影在55页、1540单元、999对照下为scoped negative；连续三轮无候选后切换方法族，H012复现LP1/06的reverse+3来源回归（209/209、32个随机仿射对照全部negative），不外推LP2。详见 reviews/auto-cycle-001.md 和 STATE.md。

停止是轮数上限，不是高优先级方向已穷尽。不得重复H009–H012的冻结配置；若继续，须新建版本并重新登记。优先候选是把H012的来源机制扩展到LP2全适用页，或选择有新机制预言的结构/钥匙族。

FEED-010新增提案（未执行）见 reviews/feed010-operational-metaphors.md：①显式网格的顺序敏感几何测量，先用保留已知对称的正负控制校准；②已知F保留不消费机制的结构检出能力，删除为另一个信息丢失对照；③有独立布局约束的外环key预测内环。两座已知5x5阵的96次机械核查已经完成（C020），只说明去重/可逆/评分不变性。不能重跑unigram评分来选螺旋方向，也不把这些提案视为已冻结未知页批次。

2026-09-13 attempt 4：H008结构诊断已完成，全部55个含符文页、84片段、12956符文；A/B/C各999对照，最低家族内校正p为0.397/0.761/0.214，均未达到0.01/3门槛。见 reviews/attempt-004.md。定位LP2/16间隔5、LP2/35边界关联及三页共同四符文片段，仅为选择后观察，不据此设五位key或猜共同单词。独立测量/算术审计35430项通过。

**H008后的候选方向（未执行）**：先用已复现的DIVINITY周期加F不消费机制检验结构测量的检出能力，再解释未知页阴性；需另冻规范和正负控制。差分关系、几何位置等仍未测试，可在有明确机制预言时单独立项。H003和H007残留状态的审阅建议保留，未升级成已经运行。

2026-09-13 attempt 3：H007-v1 已完成，55个含符文未解页、12956个符文、连续与逐页重置两个分支及8个控制job；控制与独立复核passed，主批次状态inconclusive，没有新解密页。见 reviews/attempt-003.md。第一次同规范运行的分类修正保留在 runs/attempt3-h007-v1/，正式结果见 runs/attempt3-h007-v1-corrected/。下面H001/H002保留原设计与覆盖说明，不作为待执行队列。

**H007-v1（attempt 3 已完成）**：在“明文F原样且不消费流”的假设下，已把未知跳位建模为合并的`(位置,t)`有限状态。连续跨页分支在LP2/0的ordinal 235后失去全部状态；逐页重置只在LP2/1保留5个终态、LP2/49保留1个终态且路径数为1。LP2/49是混合网格页；这些状态都不是明文验收。LP2/50无符文，不适用。

**H003（已由H009完成）**：固定语言评分器的短文本真恢复与误接纳率校准已在`runs/auto-cycle-r001-H009-v1-retry/`完成；只说明该评分工具在合成 held-out 数据上的校准表现，不构成未解页证据。不得把H007终态当作评分器证据。

2026-09-13 attempt 1：另行登记的 H006-v1 已正式运行全部 55 个含符文未解页，固定无跳位素数流、四种时钟共 220 分支／169 不同变换，未取得频率线索。见 reviews/attempt-001.md。H001/H002/H003 下列实验仍尚未执行；不要重复 H006 已覆盖配置，除非有新的输入、机制或鉴别目标。

FEED-003 方法补充：以下 H001/H002 是已知页机制校验，H003 是合成验收校准，都不是正式未解页批次。
未来有普适性的新未解假设默认统一覆盖全部适用页/区段，按 CORPUS_BATCH_TEMPLATE.json 登记，避免只围绕一页叠加方法。
主路线遵循 research/METHOD.md 的非盲目暴力策略。这里的候选设计参数不因侧聊转交而立即启动或扩张。

1. **H001（attempt 2 已完成）：F 例外是“原样保留且不消费流”，不是“原样保留但消费流”。**
   在 LP2/56 比较无跳位、跳位且不消费、跳位仍消费、所有密文 F 跳位四种预注册策略；
   用同一固定密钥在已知 LP1 DIVINITY 片段增加回归。跳位必须来自来源或预注册的枚举，不能从输出临时挑位置。
   成功标准是每页每个符文一致；不满足仅排除该页该策略。鉴别价值：分清数据例外与密钥时钟语义，避免后半页系统性错位。

2. **H002（attempt 2 已完成）：56 页的素数时钟跨行、跨标题/正文、跨散列块连续，只消费非例外符文。**
   固定 shift=1、offset=0、skip=56，预注册 6 个策略：全页连续、逐行重置、段落重置、标题后重置、
   散列字符也推进、分隔符也推进。记录第一个分歧位置与总分歧数，不用语言评分选优。
   鉴别价值：明确哪些结构属于密码时钟，哪些只是版面；这些约定必须先有回归证据，才能移到未解页。

3. **H003：固定单符文评分器对短素数流密文的真恢复，能够与错误密钥族的最高分区分。**
   当前长文本成功不足以支持短片段搜索。下一轮另写一组未见文本，固定 256 符文；20 个匹配素数流正例、
   20 个独立均匀随机流加密负例，每例固定 928 候选，总 37,120；不使用 LP 未解页。
   阈值先在额外校准集固定并哈希，再开放评估集，预注册真密钥精确恢复率和负例误接纳率。
   暂定验收 ≥18/20 精确恢复且 ≤1/20 误接纳，报告小样本不确定性。
   鉴别价值：检验“最高语言分”是否有拒绝能力，避免每次都返回一个貌似有意义的候选。

H001/H002/H007/H009–H012 的冻结JSON和实际结果已存在；历史段落中关于“H003仍未执行”的文字由上方当前收束节覆盖，不再作为待执行项。
