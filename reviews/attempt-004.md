# Attempt 4 / H008：先找结构线索，再决定往哪里下钻

2026-09-13。正式运行完成，状态 **negative**：三个登记的结构家族都没有达到线索门槛；没有新密钥或解密页。这不说明密文没有结构，也不排除其他机制。

本轮来自用户“通过一些别的测试找到蛛丝马迹，再下钻”的直接请求。原话保存在 `sources/feed008/user-request.txt`，归类为方法偏好及执行授权，不能当作密码学证据。H007 / Attempt 3 同期由另一个任务处理；本轮使用独立的 H008 / Attempt 4 名称，保留其全部文件和结果。

## 为什么换这个角度

前两轮分别检查固定素数流和已知跳位/时钟规则。本轮先问更小的问题：原始符文的顺序、分隔符位置、跨页重复是否留下可复核的异常？这些测试即使不知道明文和 key 也能运行。若出现异常，可以再提出具体机制及反例；若没有，保留实际覆盖，避免把最高分本身当作发现。

正式统计前冻结 `hypotheses/H008-structure-v1.json`，SHA-256 为 `1be41bc0b2e5ac162badfb6af43005e8e11a86468c1cb035f01198327930d536`。不使用字典、语言模型补写、GP 素数表示、密钥枚举或猜词评分。

## 输入和控制

LP2/0..55 全部进入覆盖表，55 个含符文页、12,956 个 0..28 序号实际运算。LP2/50 仅有字母数字网格，明确不适用；其余页里的数字、网格、引号等保留，不转换成符文。

固定语料 `data/attempt1-corpus-v1.json` 的 SHA-256 为 `adbc565d01546ddb24efc80c6c8ea6a0da3dfeafb5fa751b3bf94a7ec1bad519`。保留源转写版本、CR/LF、token ID、页内/全源偏移、原图路径与哈希。本轮划分84个符文分析片段，不把它们称作词或章节。

`/` 是图像行界，CR/LF 是源文件排版；二者均可能出现在一个词中。短距离配对与四符文窗口允许跨过 `- . , /`，在段落/区段标记和未知字面符号处截断；不跨页。边界测试只检查上一符文之后是否出现字面 `-`，排除每个片段的第一个位置。

| 家族 | 固定检查 | 对照 |
|---|---|---|
| A 短距离重复 | 每页间隔1..29的相等符文对，共1,595项 | 每页独立打乱，保留各符文数量和位置边界 |
| B 分隔符关系 | 每页符文身份与连字符后位置的29×2 G统计，共55项 | 每片段独立循环平移符文，固定边界掩码 |
| C 跨页片段 | 四符文相同、来自不同页的窗口配对总数，整库1项 | 与A共享页内打乱对照，计入各页重复出现的乘数 |

每类999组对照，固定种子33010804。A/B用观察加对照的对称标准化和全家族maxT；三个家族再用Bonferroni，家族内校正p须≤0.01/3才登记为正式结构线索。不是解密验收。

没有运行Atbash/整体移位等相关视图；A/B在每页独立的一一重标号下不变，C只在所有页采用同一重标号时不变。A与C相关，不能重复计为两份独立密码证据。

## 实际结果

2026-09-13 10:51:03–10:51:16 UTC执行，统计10.172秒、整轮13.281秒；999组全部完成，程序退出0，无error/timeout。正式运行核验98个固定来源文件。实际12,705个合法四符文窗口参与C家族。

| 家族 | 最显眼的观察 | 家族内校正p | 判定 |
|---|---|---:|---|
| A | LP2/16，间隔5，共20对相等符文，Z=4.0014 | 0.397 | 未通过 |
| B | LP2/35，G=49.0717，Z=2.1466 | 0.761 | 未通过 |
| C | 全库121对跨页四符文重复、119种片段 | 0.214 | 未通过 |

A的20对相对该列对照均值约8.699显得突出；但扫描1,595项后，999组随机整库中有396组出现至少这么强的家族最高值。不能据此锁定“五位密钥”。B也没有给出分隔符依赖证据。

C的随机对照均值112.070对，中央95%范围约91–133对，实际121对落在其中。此范围只描述本轮对照分布，不是密码机制的置信区间。这里报告的是家族内校正p；三家族比较通过门槛实现，不能把0.397等称作已乘3的p。

## 下钻：一个看似值得追的重复片段

按照预先固定的定位规则，选择跨页配对最多的一个四符文片段（同分按序号排序），得到 `ᛝᛠᚠᚾ`，序号 `[21,28,0,9]`。没有把拉丁读音解释成明文。

| 位置，均为零基符文ordinal | 原转写中的片段 | 图像文字行 |
|---|---|---:|
| LP2/2，47..50 | `ᛝᛠᚠᚾ` | 3 |
| LP2/22，126..129 | `ᛝᛠ-ᚠᚾ` | 6 |
| LP2/28，126..129 | `ᛝᛠᚠᚾ` | 6 |

三次出现形成三对跨页匹配。对每一对向左右延长，最长仍只有四个符文，两侧没有接着匹配；第22页中间还有一个词分隔点（源转写为 `-`），其他两处没有。查看三张固定原图时，这12个字形及第22页的分隔点与所选转写位置相符；这是局部视觉核对，不是55页逐字形校勘。

因此保留这个观察，但不给“共同单词”“共享key”或“密码重置”标签。第22、28页恰好同为ordinal126也没有登记独立位置统计，不能追加算作显著性证据。A的20对逐个定位、B的完整29×2计数、C全部119个片段及所选片段扩展均保存于 `drilldown.json`。

## 检验边界和后续

独立程序复算原始A/B/C、全部对照标准化/maxT/p和原始位置映射，属于测量与实现审计。此版本使用了完整语料，没有留出未知页符文；在这些相同数据上临时加评分仍是选择后的探索，不能叫独立密码学确认。

随机化结论依赖登记的整体页内可交换性或片段旋转零假设；没有证明任意真假页混合情况下的强家族错误率控制。B的循环连接处是零模型假设，不是加密时钟结论。IC和符文数量只作为描述性画像保存，不能据此称密文均匀、随机或不可破。

本轮没有依据把第16页、间隔5或三页四符文匹配升级为优先密钥搜索。一个可反驳的后续方向是先检验测量能力：已精确复现的DIVINITY周期加F不消费规则，在相近篇幅下是否能被这些结构指标检出？另行冻结正负控制和验收后才运行；本轮没有执行该校准。

仍未测试：间隔大于29、差分而非相等关系、跨页连续状态、GP数值结构、几何遍历、非符文网格、转写歧义分支。它们不能写作失败。本轮增加的是一套可复核的结构测量和有范围的阴性证据，不是对全部结构思路的否定。

## 复核与文件

- 原始执行：`runs/attempt4-H008-v1/record.json`、`execution.json`、`runner.stdout.txt`、`runner.stderr.txt`。
- 全量统计：`statistics.json`、`null-statistics.npz`，保留999组原始对照、标准化值和家族最大值。
- 全页覆盖与定位：`coverage.json`、`prepared-inputs.json`、`drilldown.json`。
- 输入与执行代码快照：`reproduction-bundle.zip`，在真实统计开始前写入68个成员。
- 独立算术/位置审计：`reviews/attempt-004-audit.json`；图像抽查：`reviews/attempt-004-image-check.json`。
- 开发验证：`runs/attempt4-development-v1`，17项解析和统计测试通过。
- 完整仓库回归：`runs/20260913T105657.765127Z/record.json`为passed，76项测试和6个子命令均通过，核验收尾时99个来源，未重跑H008未知页扫描。
- 完整本轮归档：`runs/attempt4-H008-v1/complete-artifacts.zip`；校验记录见 `reviews/attempt-004-archive-check.json`。包括冻结输入ZIP、实际统计与原始日志、审计和本轮研究记录；文件路径相对仓库根目录。

原图、原转写及既有运行均未覆盖；没有后台计算、定时任务、联网下载或付费资源。
